# Supabase directory
Contains migrations, policies, triggers, functions, views, materialized views, and seeds.
- Run migrations in order.
- Keep RLS enabled and policies carefully reviewed before exposing tables.
- Triggers enforce referential integrity without using foreign keys.
